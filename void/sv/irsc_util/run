#!/bin/sh
# irsc_util - Security config (runs once at boot)
exec 2>&1

sv check dbus > /dev/null || exit 1

# One-shot service - run once then sleep forever
/usr/sbin/irsc_util /etc/sec_config
exec sleep infinity
